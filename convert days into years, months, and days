#include <stdio.h>
int main() {
    int d, y, m, r;
    scanf("%d", &d);
    y = d / 365;
    d = d % 365;
    m = d / 30;
    r = d % 30;
    printf("Years = %d\n", y);
    printf("Months = %d\n", m);
    printf("Days = %d", r);
    return 0;
}
